[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "db",
    "httpx>=0.28.1",
    "pipecat-ai[deepgram,google,livekit,silero]>=1.7.0",
    "python-dotenv>=1.2.3",
]

[tool.uv.sources]
db = { path = "../db", editable = true }
